24/04
i am currently working on parseInput() function in order to grab the line of string
and separate it into values 'a', 'operator', 'b' so we could do the operation.

setOperator(), does not decide the calculation unlike previous prototype,
but insead makes button add the selected operator within the input as a string. 
    - needs improvement in terms of not allowing multiple operators to be
    selected/added